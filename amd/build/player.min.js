// Player AMD entry point — Phase C placeholder.
// AMD-wrapped (define()) — hand-written since the dev environment lacks grunt amd.
// See amd/src/player.js for rationale.

define([], function() {
    'use strict';
    return {
        init: function(cmId) {
            return cmId;
        }
    };
});
//# sourceMappingURL=player.min.js.map
